这也是 LabView 的入门课程。

组件类型

如何防止组件

在 front panel 上right click 组件会显示 visible items，可以选择label, caption, 等等一系列内容，我没搞懂label 和 caption啥区别

properties 属性可以修改 appearance

程序面板可以right click组件view as icon切换。front panel上同一种组件不同style看起来不一样，但是在block diagram上取消 view as icon 是一样的

---

学习一下常用工具：

View -> Tools palette 这个可以让根据cursor位置自动切换工具，或者手动选择一个工具

control/command+e快速切换前后面板

下面开始练习例题：闪烁周期为1s的灯，50%占空比

循环结构

我发现 block diagram 邮件显示的 functions，front panel显示controls。好像不完全一样。比如block里面有我们要的 structure-循环结构

ctrl + H 或者 comand+shift+H是现实帮助信息。为啥我查看详细信息就跳转到网页了，不知道是不是2023的问题

command+B remove broken wires 清楚断线

上下左右对齐等等

如何复制程序段？ctrl/command+CV可以，或者windows是control mac是option+左键按住拖拽（推荐后者）
在while的边框上可以右键Add Shift Register添加移位寄存器

command + U 可以cleanup 整理选中的程序，建议写一些整理一些。

程序框图禁用结构，可以临时禁用一部分内容。删除的时候不要全选+delete，而是应该右键边框选择remove

下面看另一种实现循环闪烁的方法

省流：并行执行，我感觉那个现实数据流很有趣

---

波形图/波形图表，都是front panel上的graph，分别是waveform chart和waveform graph

---

07 - 数组、簇、波形

复合数据类型

数组是同类型的集合、簇是不同类型，类似C的结构体

对于输入控件，可以右键 Data Operations - Make Current Value Default 设置当前值为默认值。这样关闭VI后再打开仍然可以保留设置的数值。

Unbundle By Name 可以unwrap

波形 build waveform 四个部分组成，可以当作特殊的簇，分别是t0, dt, Y, 属性
