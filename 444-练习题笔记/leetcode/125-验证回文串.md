---
id: 125
title: 验证回文串
link: https://leetcode.cn/problems/valid-palindrome/
difficulty: Easy
mastered: false
tags:
  - two-pointers
  - string
review_dates:
---

## 题目描述 (Problem Description)

如果在将所有大写字符转换为小写字符、并移除所有非字母数字字符之后，短语正着读和反着读都一样。则可以认为该短语是一个 **回文串** 。

字母和数字都属于字母数字字符。

给你一个字符串 `s`，如果它是 **回文串** ，返回 `true` ；否则，返回 `false` 。

**示例 1：**

**输入:** s = "A man, a plan, a canal: Panama"
**输出：**true
**解释：**"amanaplanacanalpanama" 是回文串。

**示例 2：**

**输入：**s = "race a car"
**输出：**false
**解释：**"raceacar" 不是回文串。

**示例 3：**

**输入：**s = " "
**输出：**true
**解释：**在移除非字母数字字符之后，s 是一个空字符串 "" 。
由于空字符串正着反着读都一样，所以是回文串。

**提示：**

- `1 <= s.length <= 2 * 105`
- `s` 仅由可打印的 ASCII 字符组成

## 首次尝试思路 (My Initial Attempts)

### 第一次尝试 (Attempt 1)

- **我的思路:**
  - 遍历原字符串，将大写字母变成小写字母，然后移除数字字符或者空格，得到新字符串
  - 检查新字符串是否回文

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        filtered_s = ""

        for ch in s:
            if ch.isalnum():
                filtered_s += ch.lower()

        return filtered_s == filtered_s[::-1]
```

- **结果:** 正确

- **反思与失败原因:**
  - 写法空间效率不够高（击败50%），也没有体现出题目想要考察的双指针
  - 而且都用python的方法了，可以写的更加pythonic一点：

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        filtered_s = ''.join(ch.lower() for ch in s if ch.isalnum())
        return filtered_s == filtered_s[::-1]
```

### 第二次尝试 (Attempt 2)

- **我的思路:**
  - 双指针，一个指针从左往右移动，另一个指针从右往左移动
  - 每次先动左指针，如果左指针指向非字母，那么就继续向右移动直到遇到字母字符
  - 然后同样的规则向左移动右指针
  - 比较两个指针的字符是不是同一个字母
  - 重复直到左指针位置超过右指针

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        left = 0
        right = len(s) -1

        while left <= right:
            while not s[left].isalnum():
                left += 1
            while not s[right].isalnum():
                right -= 1

            if s[left].lower() != s[right].lower():
                return False
            left += 1
            right -= 1
        return True
```

- **结果:** WA

对于输入：`" "`，出错：

```text
IndexError: string index out of range
              ~^^^^^^
    while not s[left].isalnum():
Line 7 in isPalindrome (Solution.py)
          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    ret = Solution().isPalindrome(param_1)
Line 41 in _driver (Solution.py)
    _driver()
Line 56 in <module> (Solution.py)
```

- **反思与失败原因:**
  - 注意数组下标约束范围，调整如下：

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        left = 0
        right = len(s) -1

        while left <= right:
            while left < right and not s[left].isalnum():
                left += 1
            while left < right and not s[right].isalnum():
                right -= 1

            if s[left].lower() != s[right].lower():
                return False
            left += 1
            right -= 1
        return True
```

这样的方法执行效率仍然很高，但是内存占用变低了。

## 最优解法分析 (Analysis of Optimal Solution)

- **核心思路 (Hash Table):**
  我们直接在原字符串 s 上使用双指针。在移动任意一个指针时，需要不断地向另一指针的方向移动，直到遇到一个字母或数字字符，或者两指针重合为止。也就是说，我们每次将指针移到下一个字母字符或数字字符，再判断这两个指针指向的字符是否相同。

- **思路对比 (Comparison with My Attempts):**
  - 和我的第二次尝试一样

- **复杂度分析:**
- 时间复杂度：O(∣s∣)，其中 ∣s∣ 是字符串 s 的长度。
- 空间复杂度：O(1)。

## 代码实现 (Implementation)

### Python

```python
class Solution:
    def isPalindrome(self, s: str) -> bool:
        n = len(s)
        left, right = 0, n - 1

        while left < right:
            while left < right and not s[left].isalnum():
                left += 1
            while left < right and not s[right].isalnum():
                right -= 1
            if left < right:
                if s[left].lower() != s[right].lower():
                    return False
                left, right = left + 1, right - 1

        return True
```

### C++

```cpp
class Solution {
public:
    bool isPalindrome(string s) {
        int n = s.size();
        int left = 0, right = n - 1;
        while (left < right) {
            while (left < right && !isalnum(s[left])) {
                ++left;
            }
            while (left < right && !isalnum(s[right])) {
                --right;
            }
            if (left < right) {
                if (tolower(s[left]) != tolower(s[right])) {
                    return false;
                }
                ++left;
                --right;
            }
        }
        return true;
    }
};
```

## 核心要点与易错点 (Key Takeaways & Pitfalls)

- **核心思想:** 原地判断
- **易错点:**
  1. 要注意内层循环的`left < right`这个限制

## 关联与延申 (Connections & Extensions)

- **题型模板:** 无
- **相关题目:**
  TODO

## 复盘记录 (Review Log)

TODO
