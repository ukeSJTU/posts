计算机系统是由硬件和系统软件组成的。虽然系统的具体实现方式随着时间不断变化，但是系统内在的概念却没有变化。

```c
#include <stdio.h>

int main() 
{
	printf("hello, world\n");
	return 0;
}
```

我们通过跟踪hello程序的生命周期来开始对系统的学习——从它被程序员创建开始，到在系统上运行，输出简单的消息，然后终止。

## 1.1 信息就是位+上下文
源程序（源文件）source code(source file)，也就是通过编辑器创建并保存的文本文件。文件名`hello.c`。

计算机是二进制系统，所有的东西最终都可以用`0`和`1`的组合来表示。我们将`0`和`1`统称为位（比特bit），8个位组成一组，字节byte。每个字节表示程序中的某些文本字符。源程序实际上就是位序列。

ASCII编码，用一个唯一的单字节大小的整数值表示每一个字符。
[阮一峰博客](https://www.ruanyifeng.com/blog/2007/10/ascii_unicode_and_utf-8.html)
[知乎讨论](https://zhuanlan.zhihu.com/p/339012439)

TODO：关联其他的常用编码以及相关知识。书本脚注：更多编码方式参见2.1.4节的旁注。

计算机系统中的文件可以分为两类，一类是像hello.c这样的文本文件，其他的就是二进制文件。

系统中的所有信息都是一串比特表示的，计算机需要根据上下文区分其真实含义。一个同样的字节序列可能表示一个整数、浮点数、字符串或者机器指令。

程序员需要了解数字的机器表示方法，因为它们与实际的整数和实数是不同的。它们是对真值的有限近似值，有时候会有一意想不到的行为表现。TODO：关联第二章
## 1.2 程序被其他程序翻译成不同的格式
C是一种高级语言，由它编写的源程序需要转化为一系列的低级机器语言指令，然后按照一种称为“可执行目标程序”的格式打好包，并以二进制磁盘文件的形式存放起来。目标程序也成为可执行目标文件。

[wikipedia-# 进程](https://zh.wikipedia.org/wiki/%E8%A1%8C%E7%A8%8B)