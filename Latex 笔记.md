# 数学和逻辑符号
## 二元运算符

| 符号 | LaTeX命令 | 符号 | LaTeX命令 |
|------|-----------|------|-----------|
| $+$ | `+` | $-$ | `-` |
| $\times$ | `\times` | $\div$ | `\div` |
| $\pm$ | `\pm` | $\mp$ | `\mp` |
| $\triangleleft$ | `\triangleleft` | $\triangleright$ | `\triangleright` |
| $\ast$ | `\ast` | $\star$ | `\star` |
| $\cup$ | `\cup` | $\cap$ | `\cap` |
| $\sqcup$ | `\sqcup` | $\sqcap$ | `\sqcap` |
| $\vee$ | `\vee` | $\wedge$ | `\wedge` |
| $\circ$ | `\circ` | $\bullet$ | `\bullet` |
| $\oplus$ | `\oplus` | $\ominus$ | `\ominus` |
| $\otimes$ | `\otimes` | $\oslash$ | `\oslash` |
| $\odot$ | `\odot` | $\oint$ | `\oint` |
| $\triangledown$ | `\triangledown` | $\triangle$ | `\triangle` |
| $\lhd$ | `\lhd` | $\rhd$ | `\rhd` |
| $\unlhd$ | `\unlhd` | $\unrhd$ | `\unrhd` |

## 二元关系符

| 符号        | LaTeX命令   | 符号          | LaTeX命令     |
| --------- | --------- | ----------- | ----------- |
| $<$       | `<`       | $>$         | `>`         |
| $=$       | `=`       | $\leq$      | `\leq`      |
| $\geq$    | `\geq`    | $\equiv$    | `\equiv`    |
| $\ll$     | `\ll`     | $\gg$       | `\gg`       |
| $\prec$   | `\prec`   | $\succ$     | `\succ`     |
| $\preceq$ | `\preceq` | $\succeq$   | `\succeq`   |
| $\sim$    | `\sim`    | $\approx$   | `\approx`   |
| $\simeq$  | `\simeq`  | $\cong$     | `\cong`     |
| $\asymp$  | `\asymp`  | $\neq$      | `\neq`      |
| $\doteq$  | `\doteq`  | $\propto$   | `\propto`   |
| $\models$ | `\models` | $\perp$     | `\perp`     |
| $\mid$    | `\mid`    | $\parallel$ | `\parallel` |
| $\bowtie$ | `\bowtie` | $\Join$     | `\Join`     |
| $\smile$  | `\smile`  | $\frown$    | `\frown`    |
| $\vdash$  | `\vdash`  | $\dashv$    | `\dashv`    |

## 箭头符号

| 符号 | LaTeX命令 | 符号 | LaTeX命令 |
|------|-----------|------|-----------|
| $\leftarrow$ | `\leftarrow` | $\rightarrow$ | `\rightarrow` |
| $\Leftarrow$ | `\Leftarrow` | $\Rightarrow$ | `\Rightarrow` |
| $\leftrightarrow$ | `\leftrightarrow` | $\Leftrightarrow$ | `\Leftrightarrow` |
| $\mapsto$ | `\mapsto` | $\hookleftarrow$ | `\hookleftarrow` |
| $\leftharpoonup$ | `\leftharpoonup` | $\leftharpoondown$ | `\leftharpoondown` |
| $\rightleftharpoons$ | `\rightleftharpoons` | $\longleftarrow$ | `\longleftarrow` |
| $\Longleftarrow$ | `\Longleftarrow` | $\longrightarrow$ | `\longrightarrow` |
| $\Longrightarrow$ | `\Longrightarrow` | $\longleftrightarrow$ | `\longleftrightarrow` |
| $\Longleftrightarrow$ | `\Longleftrightarrow` | $\longmapsto$ | `\longmapsto` |
| $\hookrightarrow$ | `\hookrightarrow` | $\rightharpoonup$ | `\rightharpoonup` |
| $\rightharpoondown$ | `\rightharpoondown` | $\leadsto$ | `\leadsto` |
| $\uparrow$ | `\uparrow` | $\downarrow$ | `\downarrow` |
| $\Uparrow$ | `\Uparrow` | $\Downarrow$ | `\Downarrow` |
| $\updownarrow$ | `\updownarrow` | $\Updownarrow$ | `\Updownarrow` |
| $\nearrow$ | `\nearrow` | $\searrow$ | `\searrow` |
| $\swarrow$ | `\swarrow` | $\nwarrow$ | `\nwarrow` |

## 其他符号

| 符号 | LaTeX命令 | 符号 | LaTeX命令 |
|------|-----------|------|-----------|
| $\dots$ | `\dots` | $\cdots$ | `\cdots` |
| $\vdots$ | `\vdots` | $\ddots$ | `\ddots` |
| $\forall$ | `\forall` | $\exists$ | `\exists` |
| $\neg$ | `\neg` | $\flat$ | `\flat` |
| $\natural$ | `\natural` | $\sharp$ | `\sharp` |
| $\backslash$ | `\backslash` | $\partial$ | `\partial` |
| $\infty$ | `\infty` | $\Box$ | `\Box` |
| $\Diamond$ | `\Diamond` | $\triangle$ | `\triangle` |
| $\clubsuit$ | `\clubsuit` | $\diamondsuit$ | `\diamondsuit` |
| $\heartsuit$ | `\heartsuit` | $\spadesuit$ | `\spadesuit` |
| $\top$ | `\top` | $\bot$ | `\bot` |
| $\angle$ | `\angle` | $\measuredangle$ | `\measuredangle` |
| $\sphericalangle$ | `\sphericalangle` | $\surd$ | `\surd` |
| $\diamondsuit$ | `\diamondsuit` | $\spadesuit$ | `\spadesuit` |
| $\wp$ | `\wp` | $\ell$ | `\ell` |
| $\emptyset$ | `\emptyset` | $\aleph$ | `\aleph` |

# 希腊字母
## 小写希腊字母

| 符号 | LaTeX命令 | 示例 |
|------|-----------|------|
| $\alpha$ | `\alpha` | |
| $\beta$ | `\beta` | |
| $\gamma$ | `\gamma` | |
| $\delta$ | `\delta` | |
| $\epsilon$ | `\epsilon` | |
| $\varepsilon$ | `\varepsilon` | |
| $\zeta$ | `\zeta` | |
| $\eta$ | `\eta` | |
| $\theta$ | `\theta` | |
| $\vartheta$ | `\vartheta` | |
| $\iota$ | `\iota` | |
| $\kappa$ | `\kappa` | |
| $\lambda$ | `\lambda` | |
| $\mu$ | `\mu` | |
| $\nu$ | `\nu` | |
| $\xi$ | `\xi` | |
| $\pi$ | `\pi` | |
| $\varpi$ | `\varpi` | |
| $\rho$ | `\rho` | |
| $\varrho$ | `\varrho` | |
| $\sigma$ | `\sigma` | |
| $\varsigma$ | `\varsigma` | |
| $\tau$ | `\tau` | |
| $\upsilon$ | `\upsilon` | |
| $\phi$ | `\phi` | |
| $\varphi$ | `\varphi` | |
| $\chi$ | `\chi` | |
| $\psi$ | `\psi` | |
| $\omega$ | `\omega` | |

## 大写希腊字母

| 符号         | LaTeX命令    | 示例  |
| ---------- | ---------- | --- |
| $\Gamma$   | `\Gamma`   |     |
| $\Delta$   | `\Delta`   |     |
| $\Theta$   | `\Theta`   |     |
| $\Lambda$  | `\Lambda`  |     |
| $\Xi$      | `\Xi`      |     |
| $\Pi$      | `\Pi`      |     |
| $\Sigma$   | `\Sigma`   |     |
| $\Upsilon$ | `\Upsilon` |     |
| $\Phi$     | `\Phi`     |     |
| $\Psi$     | `\Psi`     |     |
| $\Omega$   | `\Omega`   |     |

## 特殊希腊字母符号

| 符号         | LaTeX命令    |
| ---------- | ---------- |
| $\hbar$    | `\hbar`    |
| $\ell$     | `\ell`     |
| $\nabla$   | `\nabla`   |
| $\partial$ | `\partial` |

# 分数、导数和模运算
## 1. 分数 (Fractions)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $\frac{a}{b}$ | `\frac{a}{b}` | |
| $x\frac{dy}{dx}$ | `x\frac{dy}{dx}` | |
| $\partial t$ | `\partial t` | |
| $\frac{\partial y}{\partial x}$ | `\frac{\partial y}{\partial x}` | |
| $\nabla\psi$ | `\nabla\psi` | |
| $\frac{\partial^2}{\partial x_1\partial x_2}y$ | `\frac{\partial^2}{\partial x_1\partial x_2}y` | |
| $a+\frac{1}{b+\frac{1}{c}}$ | `a+\frac{1}{b+\frac{1}{c}}` | |
| $x = a_0 + \frac{1}{a_1 + \frac{1}{a_2 + \frac{1}{a_3 + a_4}}}$ | `x = a_0 + \frac{1}{a_1 + \frac{1}{a_2 + \frac{1}{a_3 + a_4}}}` | |

## 2. 导数 (Derivatives)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $\dot{a}$ | `\dot{a}` | |
| $\ddot{a}$ | `\ddot{a}` | |
| $f'$ | `f'` | |
| $f''$ | `f''` | |
| $f^{(n)}$ | `f^{(n)}` | |

## 3. 模运算 (Modular arithmetic)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $a \bmod b$ | `a \bmod b` | |
| $x \equiv a \pmod{m}$ | `x \equiv a \pmod{m}` | |
| $\gcd(m,n)$ | `\gcd(m,n)` | |
| $\operatorname{lcm}(m,n)$ | `\operatorname{lcm}(m,n)` | |

# 根式和上下标
## 1. 根式 (Radicals)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $\sqrt{x}$ | `\sqrt{x}` | |
| $\sqrt[n]{x}$ | `\sqrt[n]{x}` | |

## 2. 上下标 (Superscripts and Subscripts)

| 表达式            | LaTeX命令        | 示例  |
| -------------- | -------------- | --- |
| $x^a$          | `x^a`          |     |
| $x_a$          | `x_a`          |     |
| $x^a_b$        | `x^a_b`        |     |
| ${}^b_ax$      | `{}^b_ax`      |     |
| $\binom{n}{k}$ | `\binom{n}{k}` |     |

# ？
## 极限 (Limits)

| 表达式                    | LaTeX命令                | 示例                             |
| ---------------------- | ---------------------- | ------------------------------ |
| $\lim_{x \to a}$       | `\lim_{x \to a}`       | $\lim_{x \to a} f(x)$          |
| $\lim_{x \to \infty}$  | `\lim_{x \to \infty}`  | $\lim_{x \to \infty} f(x)$     |
| $\lim_{x \to -\infty}$ | `\lim_{x \to -\infty}` | $\lim_{x \to -\infty} f(x)$    |
| $\lim_{x \to 0}$       | `\lim_{x \to 0}`       | $\lim_{x \to 0} f(x)$          |
| $\max_{\square}$       | `\max_{\square}`       | $\max_{x \in \mathbb{R}} f(x)$ |
| $\min_{\square}$       | `\min_{\square}`       | $\min_{x \in \mathbb{R}} f(x)$ |

## 对数和指数 (Logarithms and exponentials)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $\log_a b$ | `\log_a b` | $\log_2 8 = 3$ |
| $\ln b$ | `\ln b` | $\ln e = 1$ |
| $\lg b$ | `\lg b` | $\lg 100 = 2$ |
| $\exp a$ | `\exp a` | $\exp(\ln x) = x$ |

## 界限 (Bounds)

| 表达式 | LaTeX命令 | 示例 |
|--------|-----------|------|
| $\min x$ | `\min x` | $\min_{x \in [0,1]} x^2$ |
| $\max y$ | `\max y` | $\max_{y \in \mathbb{R}} \sin y$ |
| $\sup f$ | `\sup f` | $\sup_{x > 0} \frac{1}{x}$ |
| $\inf s$ | `\inf s` | $\inf_{s \in \mathbb{Q}} \|s\|$ |
| $\lim_{u \to a}$ | `\lim_{u \to a}` | $\lim_{u \to 0} \frac{\sin u}{u}$ |
| $\limsup_{w \to \infty}$ | `\limsup_{w \to \infty}` | $\limsup_{w \to \infty} a_w$ |
| $\liminf_{v \to \infty}$ | `\liminf_{v \to \infty}` | $\liminf_{v \to \infty} b_v$ |
| $\dim p$ | `\dim p` | $\dim V$ |
| $\ker \phi$ | `\ker \phi` | $\ker T$ |