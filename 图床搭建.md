选取方案：piclist + cloudflare R2

先在cloudflare上面创建一个oss存储桶

create bucket随便起一个名字，我这里叫做picture-bed

如果创建成功，我们就可以在cloudflare的页面上面直接上传文件（图片）或者查看已经上传的文件内容。但是这个不够方便，我搭建这个图床是为了在写markdown笔记的时候可以讲添加的图片存储到这个图床上，所以我们需要piclist来在本地提供一个上传接口+返回上传的文件对应链接。

下载piclist软件，网址：https://github.com/Kuingsmile/PicList

参考这个 安装说明的部分 https://github.com/Kuingsmile/PicList?tab=readme-ov-file#download-and-install

我选择piclist而不是picgo是因为后续步骤要用到一个s3的插件我在picgo里面一直无法安装，但是piclist就可以。我觉得二者界面功能看起来都非常接近，所以选择piclist
我是mac系统，选择homebrew安装：

```bash
brew install --cask piclist
```

安装好后结果如下：

```bash
==> Downloading https://release.piclist.cn/latest/PicList-2.9.7-arm64.dmg
############################################################################################################################################################################################ 100.0%
==> Installing Cask piclist
==> Moving App 'PicList.app' to '/Applications/PicList.app'
🍺  piclist was successfully installed!
```

启动piclist

piclist默认不会显示窗口，在屏幕左上角苹果图标右边找到"PicList"，然后选择“打开主窗口”，界面如下：

![piclist主窗口](https://pub-cc2ebc8a43754210aa734d07c4898ad1.r2.dev/2025/03/piclist主窗口.png)

左边选择插件，搜索“s3”并安装。安装后界面如下(s3插件显示已经安装)

![截屏2025-03-07 11.02.03](https://pub-cc2ebc8a43754210aa734d07c4898ad1.r2.dev/2025/03/截屏2025-03-07%2011.02.03.png)

也可以手动安装：

```bash
cd <path/to/piclist>
```

这个`<path/to/piclist>`可以在设置 -> 同步与配置 -> 配置文件路径找到

然后执行

```bash
npm install picgo-plugin-s3
```

等待安装完成后，再回到piclist软件，重新载入，这个时候就应该看到插件正确加载。

到这里你应该已经安装好了s3插件，并且应该能在左侧边栏->图床中看到Amazon S3选项：你的界面可能会比我多显示一些图床选项，例如腾讯云COS，阿里云OSS等等，这是因为我在设置-上传设置-请选择显示的图床中把不需要的全部取消勾选了：

![截屏2025-03-07 11.06.34](https://pub-cc2ebc8a43754210aa734d07c4898ad1.r2.dev/2025/03/截屏2025-03-07%2011.06.34.png)

添加新的配置，下面要填写的信息需要我们去cloudflare里面进行配置：

在cloudflare - R2 的Overview界面也就是最一开始点击Create Bucket的地方，左边有一个API按钮。选择Manage API Tokens

![截屏2025-03-07 11.09.44](https://pub-cc2ebc8a43754210aa734d07c4898ad1.r2.dev/2025/03/截屏2025-03-07%2011.09.44.png)

应该看到这样的界面。这里显示的就是我已经创建的一个API Token，你的可能什么都不显示。

点击Create API Token按钮。

Token Name可以随便起一个名字。

Permission权限必须选择Read 和 Write都有的，这里选择 Object Read & Write，不需要Admin这么高权限的。

Specify bucket选择apply to specific buckets only, 然后选择你想要用来作为图床存储图片的那个桶。

剩下暂时不需要管。然后Create API Token。随后跳转的界面上就会给出必要的信息，这些信息只会在当前页面上显示，刷新/退出页面后就看不到了，因此一定要保管好，或者在配置完成前不要关闭。

回到piclist软件开始设置Amazon S3插件的设置。

图床配置名：随便填一个名字

# 拓展功能：

## 上传的时候压缩成webp

## obsidian支持

# Tips

截图的名字最好不要包含奇怪的符号或者空格。piclist返回的图片链接里面没有经过[[URL编码]]，markdown格式是有问题的。
