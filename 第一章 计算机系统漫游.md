计算机系统由硬件和系统软件组成。系统的具体实现方式随着时间变化，但是内在的概念不会改变，这也是本章乃至本书的的内容所在。

```c
#include <stdio.h>

int main() 
{
	printf("hello, world\n");
	return 0;
}
```

从它被程序员创建开始，到在系统上运行，输出简单的消息，然后终止。TODO（是在本章中吗？）我们将沿着这个程序的生命周期，简要地介绍一些逐步出现的关键概念、专业术语和组成部份。后面的章节将围绕这些内容展开。



