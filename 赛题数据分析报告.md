本报告结合以下三份核心数据进行分析：

1. **齿轮参数文档**: `【公开】齿轮信息.docx`
2. **宏观照片**: `宏观照片/【公开】A014-4081-擦伤/IMG_20250311_143309.jpg`
3. **体式镜照片**: `体式镜/4081-19014/1.jpg`

## 文件与编号解读

通过分析文件路径和内容，可以建立以下关联：

- **核心关联**: 从路径中的编号 `4081` 来看，宏观照片与体式镜照片应为同一齿轮的拍摄图像。
- **损伤类型**: 宏观照片路径明确指出该样本的损伤类型为“**擦伤**”。
- **齿轮识别**: 编号 `4081` 对应`齿轮信息.docx`文档中第十组的从动轮`06-4081`和第十一组的主动轮`06-4081`。此推断因其他编号也能对应上而具有较高可信度。目前尚未放出的齿轮照片可能与其他无法对应上的编号相关。
- **详细编号分析 (`4081-19014`)**:
  - `4081`: 齿轮零件编号。
  - `014`: 样本或实验编号，对应宏观照片路径中的 `A014`。`A/B`前缀的含义暂不明确。
  - `19`: **推测为齿轮上第19号牙齿的编号**。

## 数据探索

### 1. 齿轮参数的简单介绍

`齿轮信息.docx`中各参数的通俗化解释如下：

- **中心距 (a, mm)**: 两个啮合齿轮各自中心轴之间的距离 3。
- **模数 (mn, mm)**: 可简单理解为“牙齿的大小” 4。模数越大，齿也越大 5。
- **齿数 (z)**: 一个齿轮上轮齿的总数 6。
- **变位系数 (x)**: 用于微调齿廓位置的设计参数 7。在本次数据中，该值多为0，表示是标准齿轮 8。
- **螺旋角 (β, °)**: 轮齿相对于齿轮轴线的倾斜角度 9。本次数据中均为0，意味着它们都是“直齿轮” 10。
- **压力角 (α, °)**: 可理解为轮齿的“倾斜度”或齿形角，影响传动性能 11。
- **齿顶圆 (da, mm)**: 齿轮最外圈（齿顶端）构成的圆的直径 12。
- **齿宽 (b, mm)**: 齿轮的厚度，即轮齿的宽度 13。
- **材料属性**: 定义了齿轮的制造材料及其物理特性，如密度、弹性模量、泊松比和屈服应力等
- **载荷数据**: 描述齿轮工作时承受的扭矩（力气大小）和转速（转动快慢）

### 2. 这些参数有什么用？

核心思路是利用这些参数进行工程计算，并将计算结果作为特征，与CNN处理后的图像特征共同送入分类器，以提升模型性能和可解释性。

#### 计算步骤：

1.  **计算作用力**: 根据扭矩和几何尺寸，可以算出作用在齿面上的切向力 $F_t$。
    $$F_t = \frac{2T}{d}$$
    其中，$T$ 是扭矩， $d$ 是分度圆直径 ($d = m_n \cdot z$)。

2.  **计算接触应力**: 使用赫兹接触应力公式计算最大压力 $\sigma_H$。
    $$\sigma_H = C_p \sqrt{\frac{F_t}{b \cdot d_1} \cdot \frac{u+1}{u}}$$
    公式中的变量均可从参数表中获得或计算得出：
    - $C_p$: 由材料的**弹性模量E**和**泊松比v**决定。
    - $F_t$: 源于**扭矩**。
    - $b$: **齿宽**。
    - $d_1$: 主动轮分度圆直径 ($m_n \cdot z_1$)。
    - $u$: 齿数比 ($z_2 / z_1$)。

#### 应用价值：

计算出的接触应力 $\sigma_H$ 等物理指标，为AI模型提供了“为什么会这样”的背景信息。例如，高应力区域更容易产生疲劳麻点。这种“多模态”输入可以帮助网络更好地理解损伤机理，从而：

- 从物理原理上提高分类准确率。
- 为深度学习模型提供可解释性。
- 在相似的损伤图像间，根据应力大小判断其恶化趋势。

### 3. 如何理解体式镜图片

体式镜（Stereoscopic Microscope）是一种能产生立体视觉效果的显微镜，用于观察物体表面细节。在本次任务中，它是能够详细观察齿轮损伤状态和类型的关键数据。

### 4. 宏观与体式镜图片的关系

两张图片在尺度上的差异决定了它们的协同作用：

- **宏观图片**: 用于**定位损伤区域**。
- **体式镜图片**: 用于**分类损伤类型**和**量化损伤程度**。

通过结合分析，可以实现更精细的判断。例如，若损伤发生在特定区域（如齿顶），则根据工业经验，某种损伤类型的概率可能会更高。此外，根据体式镜的拍摄角度和标尺，未来还有可能计算出损伤的具体面积大小，实现损伤的量化评估。
